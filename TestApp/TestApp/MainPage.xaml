<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:colorpicker="clr-namespace:ColorPicker;assembly=ColorPicker"
             mc:Ignorable="d"
             x:Class="TestApp.MainPage">

    <StackLayout>
        <ScrollView>
            <StackLayout>
                <Label Text="Color" TextColor="Black" FontSize="Large" FontAttributes="Bold" BackgroundColor="{Binding Source={x:Reference ColorWheel1}, Path=SelectedColor}"/>
                <Label Text="Color" TextColor="White" FontSize="Large" FontAttributes="Bold" BackgroundColor="{Binding Source={x:Reference ColorWheel1}, Path=SelectedColor}"/>
                <colorpicker:ColorWheel x:Name="ColorWheel1" PickerRadius="16"/>
                <colorpicker:HSLSliders x:Name="HSLSliders1" PickerRadius="16" ConnectedColorPicker="{x:Reference Name=ColorWheel1}"/>
                <colorpicker:RGBSliders x:Name="RGBSliders1" PickerRadius="16" ConnectedColorPicker="{x:Reference Name=HSLSliders1}" SelectedColor="Gray"/>
            </StackLayout>
        </ScrollView>
    </StackLayout>

</ContentPage>
